*(see file written above — adds `@app.errorhandler(Exception)` catch-all that delegates HTTPExceptions to existing handlers, logs the traceback, persists to `error_log`, and returns structured `{ error_code, request_id }` JSON)*