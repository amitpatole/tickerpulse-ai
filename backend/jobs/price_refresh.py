*(Markdown fences removed — file now begins with the module docstring and is valid Python)*

--- FILE: frontend/src/hooks/useWSPrices.ts ---
*(Markdown fences removed — file now begins with `'use client';` and is valid TypeScript)*