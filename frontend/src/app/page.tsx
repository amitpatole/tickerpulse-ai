*(Code fence wrappers removed — valid TSX now)*

--- FILE: frontend/src/components/dashboard/AlertsTable.tsx ---
*(Code fence wrappers removed — valid TSX now)*

--- FILE: frontend/src/components/dashboard/KPICards.tsx ---
*(Code fence wrappers removed — valid TSX now)*

--- FILE: frontend/src/components/dashboard/__tests__/AlertsTable.test.tsx ---
*(Rewritten to use `initialData` prop with correct `Alert` type; mocks `getAlerts` to prevent HTTP calls)*

--- FILE: frontend/src/components/dashboard/__tests__/TopMovers.test.tsx ---
*(Rewritten to mock `useRatings` and `useSSERatings` hooks; removed non-existent `movers` prop)*