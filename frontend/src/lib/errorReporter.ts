*(see file written above — adds `ErrorSeverity` type + `severity` field on `ErrorPayload`, sets `severity: 'high'` for exceptions/rejections and `severity: 'critical'` for React errors, dispatches `toast(message, 'error')` inside `_send` for critical/high)*